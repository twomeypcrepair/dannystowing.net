# AGENTS.md — dannystowing.net

Pointer file for any agent tool (Cursor, Cline, Copilot, Codex).

Read `CLAUDE.md` in this folder if present (gitignored, local only). Then read `memory\STATE.md` for where the work stands and `memory\DECISIONS.md` so settled calls aren't re-argued.
